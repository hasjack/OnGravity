name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      #############################################
      # ORIGINAL VITE BUILD STEPS (COMMENTED OUT)
      # Uncomment these if returning to client-side SPA build mode
      #############################################

      # - name: Setup Node
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      #     cache: 'yarn'
      #     cache-dependency-path: client/yarn.lock

      # - name: Install dependencies (with lockfile)
      #   if: ${{ hashFiles('client/yarn.lock') != '' }}
      #   working-directory: client
      #   run: yarn install --frozen-lockfile

      # - name: Install dependencies (no lockfile)
      #   if: ${{ hashFiles('client/yarn.lock') == '' }}
      #   working-directory: client
      #   run: yarn install

      # - name: Build Vite SPA
      #   working-directory: client
      #   env:
      #     CI: true
      #   run: yarn build

      #############################################
      # ACTIVE: DEPLOY STATIC SNAPSHOT
      #############################################

      - name: Prepare static site
        working-directory: client
        run: |
          mkdir -p dist
          cp snapshot.html dist/index.html

      #############################################

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: client/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
